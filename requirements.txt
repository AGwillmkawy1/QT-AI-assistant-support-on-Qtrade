sentence-transformers>=2.7,<4
numpy>=1.26
requests>=2.31

# Optional LLM backends (install whichever you use)
# groq>=0.9          — free tier, recommended (console.groq.com)
# anthropic>=0.30    — paid tier

